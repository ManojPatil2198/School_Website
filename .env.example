# Application Base URL (defaults to production Wix URL if not specified)
BASE_URL=https://united-school-tokyo.wixsite.com/united-school-of-tok/home

# CI indicator (automatically set to true by CI environments like GitHub Actions)
# CI=true
